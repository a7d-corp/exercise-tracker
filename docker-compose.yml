version: '3.8'

services:
  exercise-tracker:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - EXERCISES_FILE=/data/exercises.json
    volumes:
      - ./data:/data
    restart: unless-stopped

